name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  portal:
    uses: Shreai/.github/.github/workflows/reusable-node-ci.yml@main
    with:
      node-version: '20'
      working-directory: portal
