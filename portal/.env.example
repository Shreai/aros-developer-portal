# Agent Factory backend base URL.
# This is inlined by Vite at build time (prefix must be VITE_).
# Change this to the deployed shre-agent-factory URL before building for production.
VITE_AGENT_FACTORY_URL=http://localhost:8080/api/v1
